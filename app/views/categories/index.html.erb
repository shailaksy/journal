<%= render "nav" %>
<h1>Categories</h1>

<div id="categories">
  <% @categories.each do |entry| %>
    <%= render category %>
    <p>
      <%= link_to "Show this category", category %>
    </p>
  <% end %>
</div>

<%= link_to "New category", new_category_path %>